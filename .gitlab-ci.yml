image: "alpine:latest"

before_script:
  - apk add --no-cache python3
  - python3 -m venv env
  - source ./env/bin/activate
  - pip3 install -r requirements.txt

build:
  - source ./env/bin/activate
  - python3 ./web_appy.py
